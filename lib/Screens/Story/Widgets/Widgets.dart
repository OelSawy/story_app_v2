import 'package:flutter/material.dart';
import 'package:story_app_v2/Screens/Log%20In/Provider/logInProvider.dart';

/* class Widgets {
  static Widget button = InkWell(
    onTap: () {
      if (LogInProvider().verify("userName", "password")) {
        Navigator.of(context)
      }
    },
  );
} */